<match **>
    @type copy
    <store>
       @type elasticsearch
       host 10.205.0.1
       port 9200
       logstash_format true
       logstash_prefix flog
       flush_interval 15s
    </store>
</match>
